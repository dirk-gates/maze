# C/C++ build artifacts
maze
a.out

# Swift Package Manager
.build/
.swiftpm/
Package.resolved

# Xcode
DerivedData/
build/
MazeApp/build/
# xcuserdata holds per-user IDE state (window positions, breakpoints,
# scheme management). Use ** so the patterns match nested project
# locations like MazeApp/Maze.xcodeproj/...
**/xcuserdata/
**/*.xcuserdata
**/*.xcuserstate
*.swp

# macOS
.DS_Store
